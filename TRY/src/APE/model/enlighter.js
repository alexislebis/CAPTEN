/**
 * An enlighter is a structure which offers all the relevant information about a thing. It uses the
 * descriptive classes of the ontology and allows to make a relation with another thing B.
 Could be useful later
 */
